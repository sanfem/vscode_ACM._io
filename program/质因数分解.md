每一个自然数都可以唯一分解为有限个质数的乘积
试除法分解，时间复杂度是平方N。
```cpp
void divide(int n)//n is goal
{
	int m=0;
	for(int i=2;i<=sqrt(n);i++){
		if(n%i==0)
		{
			p[++m]=i;
			c[m]=0;
			while(n%i==0) n/=i,c[m]++;//delete all i
		}
	}
	if(n>1)//n is prime
		p[++m]=n,c[m]=1;
}//p 是质因数，c是质因数的个数
```
 
